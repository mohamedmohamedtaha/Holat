package com.holat.login.base.network

import androidx.lifecycle.ViewModel

class NetworkStatusViewModel (networkStatusTracker: NetworkStatusTracker) :
    ViewModel() {
//    val state = networkStatusTracker.networkStatus
//        .map(onUnavailable = { NetworkStatus.UnAvailable },
//            onAvailable = { NetworkStatus.Available })
//        .asLiveData(Dispatchers.IO)

}